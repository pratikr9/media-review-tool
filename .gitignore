# Virtual environments (never survive machine sync)
.venv/
venv/

# Python cache
__pycache__/
*.pyc

# Build output (rebuild from C:\mrt-build; exe is >100 MB, exceeds GitHub limit)
build/
dist/
*.exe

# App runtime state
logs/
review_state.json
app_settings.json

# OneDrive sync-failure artifacts
*_Error.txt
